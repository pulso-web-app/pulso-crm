# Shared UI

`shared-ui` contains CRM-local, domain-neutral presentation primitives such as the loading overlay and equal-width grid directive. Contact-specific components and behavior remain in the contacts capability.

Public imports use `@pulso-crm/shared-ui`.

Run `npx nx test shared-ui` for focused tests.
