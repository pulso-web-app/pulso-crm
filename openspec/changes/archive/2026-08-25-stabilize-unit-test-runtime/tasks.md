## 1. Unit-Test Runtime

- [x] 1.1 Configure the supported Angular application build target and verify the unsupported-builder warning is absent
- [x] 1.2 Add the test-only `ResizeObserver` setup and verify CRM tests emit no runtime error

## 2. Validation

- [x] 2.1 Run strict OpenSpec validation and `npm run check`
